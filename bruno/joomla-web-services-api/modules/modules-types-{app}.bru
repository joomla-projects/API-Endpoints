meta {
  name: modules/types/{app}
  type: http
  seq: 3
}

get {
  url: {{base_url}}/v1/modules/types/{{app}}
  body: none
  auth: inherit
}

vars:pre-request {
  app: site
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
  test("Response has data", function() {
    expect(res.body).to.have.property("data");
  });
}

example {
  name: 200 Response
  description: todo: response object
  
  request: {
    url: {{base_url}}/v1/modules/types/{{app}}
    method: GET
    mode: none
  }
  
  response: {
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}

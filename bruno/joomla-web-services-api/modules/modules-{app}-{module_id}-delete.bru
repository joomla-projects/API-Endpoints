meta {
  name: modules/{app}/{module_id} (DELETE)
  type: http
  seq: 5
}

delete {
  url: {{base_url}}/v1/modules/{{app}}/{{module_id}}
  body: none
  auth: inherit
}

vars:pre-request {
  app: site
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}

example {
  name: 200 Response
  description: todo: response object
  
  request: {
    url: {{base_url}}/v1/modules/{{app}}/{{module_id}}
    method: DELETE
    mode: none
  }
  
  response: {
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}

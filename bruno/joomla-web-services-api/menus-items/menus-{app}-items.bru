meta {
  name: menus/{app}/items
  type: http
  seq: 1
}

get {
  url: {{base_url}}/v1/menus/{{app}}/items
  body: none
  auth: inherit
}

vars:pre-request {
  app: site
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
  test("Response has data", function() {
    expect(res.body).to.have.property("data");
  });
}

example {
  name: 200 Response
  description: todo: response object
  
  request: {
    url: {{base_url}}/v1/menus/{{app}}/items
    method: GET
    mode: none
  }
  
  response: {
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}

meta {
  name: Delete Banners Versions
  type: http
  seq: 2
}

delete {
  url: {{base_url}}/v1/banners/{{banner_id}}/contenthistory
  body: none
  auth: inherit
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}

example {
  name: 200 Response
  description: todo: response object
  
  request: {
    url: {{base_url}}/v1/banners/{{banner_id}}/contenthistory
    method: DELETE
    mode: none
  }
  
  response: {
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
